select  service_area_id,pricing from service_area_pricing where id in (
    select f.service_area_pricing_id from airport_fleet af
    left join fleet f on f.id = af.fleet_id
where af.parent_fleet_id ={{parent_fleet_id}} and af.airport = '{{airport}}')